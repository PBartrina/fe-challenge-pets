{
  "name": "feature-shell",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "feature-shell/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["type:feature scope:app"],
  "targets": {
    "build": {
      "executor": "@nx/angular:ng-packagr-lite",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "feature-shell/ng-package.json",
        "tsConfig": "feature-shell/tsconfig.lib.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "feature-shell/tsconfig.lib.prod.json"
        },
        "development": {}
      },
      "defaultConfiguration": "production"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "feature-shell/jest.config.ts",
        "tsConfig": "feature-shell/tsconfig.spec.json"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
